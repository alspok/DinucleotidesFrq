using System;
using System.IO;

namespace DinucleotidesFrq.Helpers
{
    public class SequenceFile
    {
        public void SeqFile(string seqName)
        {
            using (StreamReader stream = new StreamReader(seqName))
            {
            }
        }
    }
}
